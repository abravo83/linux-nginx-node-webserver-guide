# linux-nginx-node-webserver-guide
A guide to deploy multiple node-like webservers (i.e. express) under a single server using nginx as reverse proxy to redirect different domains to their own node server.
